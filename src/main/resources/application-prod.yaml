server:
  port: 9999

spring:
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.31.72:3306/spdemo?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    type: com.alibaba.druid.pool.DruidDataSource

# 自定义工程配置
project-config:
  system-name: Node01 Server数据分析平台
  upload-path: /root/uploads